import React from 'react';

export default function Login() {
  return (
    <div className="login-page">
      <!-- Copy your HTML content here -->
      <!-- Convert class to className -->
      <form className="login-form">
        <input type="email" />
        <input type="password" />
        <button>Login</button>
      </form>
    </div>
  );
}
